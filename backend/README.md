# HiveInvestor Backend

This project contains the FastAPI backend for the HiveInvestor application.

## Setup

1.  Install dependencies:
    ```bash
    pip install -r requirements.txt
    ```

2.  Run the application:
    ```bash
    uvicorn app.main:app --reload
    ```

## Testing

Run unit tests:
```bash
pytest
```
